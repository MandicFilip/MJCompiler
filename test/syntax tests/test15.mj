//should test advanced statements
program p
    const int c = 5;
{
    int f(int x)
    {
        return x;
    }

    void main()
        int x, y;
    {
        x = 10;
        x = f(x);
        read(y);
        x = x - y;
        if (x > y)
        {
            x = x + 5;
        } else
        {
            y = y + 5;
            x = x * (y - 10);
        }
        print(x);

        for(y = 5; y < x; y++)
        {
            x = x;
            continue;
            break;
        }
    }
}
